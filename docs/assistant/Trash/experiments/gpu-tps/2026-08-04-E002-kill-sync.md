# E002 — Kill blocking confirm + nomap-only + no harvest

## Meta

- **Id:** E002
- **Date:** 2026-08-04
- **Step:** 3–5
- **Preferred path still?** yes

## Hypothesis

Replacing spin-Wait counter maps with non-blocking peek + `on_submitted_work_done` gating removes sync tax; nomap-only and no harvest keep the preferred stationary path clean.

## Change

- `ProductionAtlas::poll_completion_counts` — one in-flight map, never spin.
- Cgen submit sets `gpu_done` via `on_submitted_work_done`; drain skips map until done.
- Stationary path: nomap scatter only; harvest banned when GPU-preferred + stationary.
- Fused micro-batch init+bout (8 tiles / mega buffer) + per-tile scatter `point_base`.

## Commands

```bash
taskset -c 3-8 cargo test --release --lib home_800x480_fills_gpu_path_probe -- --nocapture
```

## Metrics

| Metric | Value |
|--------|-------|
| whole-TPS | ~470 (warmed device) / ~380 cold |
| fill% | 96 |
| workshifts | 3–4 |
| ms | ~190–240 |
| compute-only IPS | ≥30B (`gpu_ips_batch_1024_meets_30b` ok) |
| maps | 2 |
| waits | 0 |
| harvests | 0 |
| cpu_bridge_publishes | 0 |
| nomap_scatters | 104 |
| mapped_scatter | 0 |
| counter_polls | ~10 |
| publish path | gpu handle / cpu_bridge=0 |

## Verdict

pass (sync tax near zero) — TPS still ≪3000

## Next

continue ladder Step 6 (D-PUB-4 bind). IPS already high → remaining wall is pipeline/residency between tiles, not Mandelbrot ALU.

## Do not retry

- `MICRO_BATCH=128` single half without immediate exhaust (stuck ~88%).
- Abandoning pending maps.
- Blocking Wait on confirm drain.
- Full-screen fused bout that co-schedules deep interior with exterior (minute-scale hangs).
