# E003 — D-PUB-4 GPU handle bypass queue

## Meta

- **Id:** E003
- **Date:** 2026-08-04
- **Step:** 6
- **Preferred path still?** yes

## Hypothesis

Stationary GPU fill already scatters Answers into the production atlas; the missing D-PUB-4 piece for headed display is forwarding those slot handles on the publisher bypass without `from_answer_tile` + CPU pack.

## Change

- `TileSession` queues `pending_gpu_publishes` on whole-tile GPU complete (alongside TPS sink).
- `tile_worker::flush_unsent_tiles` drains those handles to `bypass_out` first (no CPU pack).
- `publish_tile_cpu_bridge` remains for host-calibrated path only; probe `cpu_bridge=0`.

## Commands

```bash
taskset -c 3-8 cargo test --release --lib home_800x480_fills_gpu_path_probe -- --nocapture
```

## Metrics

| Metric | Value |
|--------|-------|
| whole-TPS | ~520 (warmed) |
| fill% | 96 |
| workshifts | 3 |
| ms | ~173 |
| maps | 2 |
| waits | 0 |
| harvests | 0 |
| cpu_bridge_publishes | 0 |
| nomap_scatters | 104 |
| publish path | gpu_bind handles queued; bridge=0 |

## Verdict

pass (impl slice) — TPS still ≪3000; headed smoke not run this step

## Next

continue ladder Steps 7–14; TPS wall is not CPU bridge

## Do not retry

Rebuilding bypass payloads via `GPUTile::from_answer_tile` + `place_on_production_atlas` when a production slot already exists.
