# E001 — Baseline sandwich + instrument

## Meta

- **Id:** E001
- **Date:** 2026-08-04
- **Step:** 1–2
- **Preferred path still?** yes

## Hypothesis

Whole-TPS is low because of host sync/residency (maps/Waits/harvest/CPU bridge), not Mandelbrot math. Instrument and baseline before algorithm pivots.

## Change

- Added `gpu_tps_tax` counters (maps, waits, harvests, cpu_bridge, nomap, mapped_scatter, counter_polls).
- Probe prints tax line.
- Experiment log scaffolding.

## Commands

```bash
taskset -c 3-8 cargo test --release --lib home_800x480_fills_gpu_path_probe -- --nocapture
```

## Metrics

| Metric | Value |
|--------|-------|
| whole-TPS | ~370–390 (pre-warmup) |
| fill% | 96 |
| workshifts | 4 |
| ms | ~233–255 |
| compute-only IPS | (see E002 / gpu_ips tests) |
| maps | ~45 (spinning try_read) |
| waits | 0 |
| harvests | 0 |
| cpu_bridge_publishes | 0 |
| nomap_scatters | ~104 |
| counter_polls | high |
| publish path | gpu handle ingest (cpu_bridge=0) |

## Verdict

pass (baselines + inventory recorded)

## Next

continue ladder — kill blocking confirm drain

## Do not retry

Blocking `PollType::Wait` on confirm drain; abandoning pending maps (map storms).
