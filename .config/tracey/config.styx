specs (
  {
    name critical-zoomer
    include (docs/tracey/*-rules.md)
    impls (
      {
        name rust
        include (src/**/*.rs)
        exclude (target/**)
        test_include (src/**/*.rs)
      }
    )
  }
)
