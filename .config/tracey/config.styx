specs (
  {
    name critical-zoomer
    include (
      docs/requirements.md
      docs/design/*.md
    )
    impls (
      {
        name rust
        include (src/**/*.rs src/**/*.wgsl scripts/**/*.sh)
        exclude (target/**)
        test_include (src/**/*.rs scripts/**/*.sh)
      }
    )
  }
)
